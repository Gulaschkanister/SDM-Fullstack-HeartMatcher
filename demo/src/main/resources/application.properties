spring.datasource.url=jdbc:mariadb://localhost:3306/demo
spring.datasource.username=sdm
spring.datasource.password=123
spring.datasource.driver-class-name=org.mariadb.jdbc.Driver
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# Notes: Configure MariaDB in WSL and create database/user before starting the app.
# Example (in WSL):
# sudo apt update && sudo apt install -y mariadb-server
# sudo systemctl start mariadb
# sudo mysql -e "CREATE DATABASE demo;"
# sudo mysql -e "CREATE USER 'demo'@'localhost' IDENTIFIED BY 'secret';"
# sudo mysql -e "GRANT ALL PRIVILEGES ON demo.* TO 'demo'@'localhost'; FLUSH PRIVILEGES;"

